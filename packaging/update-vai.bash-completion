_update_vai() {
  local cur
  cur="${COMP_WORDS[COMP_CWORD]}"
  local opts="--auto -a --yes -y --help -h"
  COMPREPLY=($(compgen -W "${opts}" -- "${cur}"))
  return 0
}
complete -F _update_vai update-vai
